# Never commit API keys or secrets
.env
*.env

# Python
__pycache__/
*.pyc

# Triage logs may contain real ticket text typed in during testing -
# keep these local, don't publish them
triage-log.jsonl
